// LIVE homepage → the 2050 "Luminescent Depth" redesign.
// The full composition + design system lives in app/v2/page.tsx; we re-export
// it here so promarketing.pw "/" renders it. The previous homepage components
// remain untouched in components/landing/* as an instant rollback path.
export { default } from "./v2/page";
